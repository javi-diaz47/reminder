import { supabase } from "./supabase";

const addMedicineDB = async ({ user_id, name }) => {
  // if (error) {
  //   console.log(error);
  // }
  // console.log(data);
  // return data;
};

export { addMedicineDB };
